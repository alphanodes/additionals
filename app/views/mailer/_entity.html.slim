hr

h1 = link_to h(title), entity_url

= render_email_attributes entity, html: true

= textilizable entity, content_field, only_path: false

- attachments = entity_mail_attachments entity, @journal
- if with_attachments && attachments.any?
  fieldset.attachments
    legend = l :label_attachment_plural
    - attachments.each do |attachment|
      = render_label_sum link_to_attachment(attachment, download: true, only_path: false), number_to_human_size(attachment.filesize)
      br
